import { render, screen } from "@testing-library/react";
import LocationBar from "./LocationBar";

describe("LocationBar", () => {
    test("LocationBar renders", () => {
        // render(
        //     <LocationContext.Provider value={{ lat: 2, lng: 2 }}>
        //         <LocationBar />
        //     </LocationContext.Provider>
        // );
        // screen.getByPlaceholderText("Where");
        // screen.getByPlaceholderText("Within");
    });
});
